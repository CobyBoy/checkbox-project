import { ChildrenCheckboxDirective } from './children-checkbox.directive';

describe('ChildrenCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildrenCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
